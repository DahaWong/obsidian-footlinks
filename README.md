1. Put `main.js` and `manifest.json` into `./YourVault/.obsidian/plugins/footlink/`, then refresh(`Ctrl/Cmd + R`) the app.
2. Turn on the toggle in `Settings -> Third-party Plugins -> Footlink`.
3. Done.
